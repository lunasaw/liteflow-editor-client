/**
 * 脚本节点定义
 */
import { NodeTypeEnum } from '../constant';
import icon from '../assets/script-icon.svg';

const config: LiteFlowNode = {
  label: '脚本',
  type: NodeTypeEnum.SCRIPT,
  icon,
};

export default config;
